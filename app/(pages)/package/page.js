import React from 'react'
import Home from './Home'

export const metadata = {
    title: 'jadeScroll title package page',
    description: 'jadeScroll package ranking page',
    openGraph: {
        title: 'jadeScroll title package page',
    },
}

function Package() {
    return (
        <div>
            <Home />
        </div>
    )
}

export default Package